#!/usr/bin/env bash

DisplayInfo() {
	echo "${B_YLW}${BLK}  IMBOT - iMessage Bot ${NORMAL}";
	echo "${BBLU}Send messages via your Mac's iMessage.app at a given interval.";
	echo "${BBLU}Optionally include a picture in your message.";
	echo "${BBLU}This is very easy to adapt to a number of situations and use cases";
	echo "${BGRN}as-send-imsg.sh    ${WHT}- Once you've configured your options, run this script to send messages until command is stopped.";
	echo "${NORMAL}";
}
