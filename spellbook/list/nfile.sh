#!/usr/bin/env bash

DisplayInfo() {
	echo "${B_YLW}${BLK}  NFILE - New File ${NORMAL}";
	echo "${BBLU}Quick script you can macro or shortcut to create a new file from a template.";
	echo "${BBLU}Such as having a standard Readme.md or new_command.sh";
	echo "${BBLU}Currently this is MacOS ONLY!";
	echo "${BGRN}NFile.app          ${WHT}- Set a keyboard macro to run this app to run via System Preferences";
	echo "${NORMAL}";
}
