#!/usr/bin/env bash

DisplayInfo() {
	echo "${B_YLW}${BLK}  ASCII - Generate text art from images  ${NORMAL}";
	echo "${BGRN}image2ansi ${WHT}- generates ansi art built from just zeros.";
	echo "${BGRN}ansize     ${WHT}- Like image2ansi but uses more than just zeros.";
	echo "${BGRN}img2txt.py ${WHT}- Image to Ascii Text with color support, can output to html or ansi terminal.";
	echo "${NORMAL}";
}
